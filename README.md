# Ftag
a simple file tagger
```
Usage: ftag [--file FILE] <command> [<args>]

Options:
  --file FILE, -f FILE   use a non-default ftag file [default: .ftag]
  --help, -h             display this help and exit

Commands:
  add                    add a file to ftag
  query                  search for a tag
```
